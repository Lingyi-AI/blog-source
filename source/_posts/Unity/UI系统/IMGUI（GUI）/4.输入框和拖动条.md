---
title: 4.输入框和拖动条
date: 2026-03-07 19:47
categories:
  - UI系统
  - IMGUI

tags: 
  - IMGUI（GUI）

---

### 知识点一 输入框

#### 普通输入

``` CSharp
//输入框 重要参数 一个是显示内容 string
//一个是 最大输入字符串的长度	//inputStr是外部声明的string变量
inputStr = GUI.TextField(new Rect(0, 0, 100, 30), inputStr, 5);
```


#### 密码输入	

``` CSharp
//外部声明的string不能为null
inputPW = GUI.PasswordField(new Rect(0, 50, 100, 30), inputPW, '*');
```


### 知识点二 拖动条

#### 水平拖动条

``` CSharp
// 当前的值
// 最小值 left
// 最大值 right	//nowValue是外部声明的float变量
nowValue = GUI.HorizontalSlider(new Rect(0, 100, 100, 50), nowValue, 0, 1);
Debug.Log(nowValue);
```


#### 竖直拖动条

``` CSharp
nowValue = GUI.VerticalSlider(new Rect(0, 150, 50, 100), nowValue, 0, 1);
```

![图](images/Unity/UI系统/IMGUI（GUI）/4.习题.png)

``` CSharp
    private void OnGUI()
    {
        if (guitext2.shezhimianbanxianshi) 
        {
            value1 = GUI.HorizontalSlider(audio1, value1, 0, 1);
            value2 = GUI.HorizontalSlider(audio2, value2, 0, 1);
        }
        audiosource.volume = value1;

        if (denglujiemian)
        {
            GUI.Label(shezhimianban, imager);
            GUI.Label(youxishezhii, "登陆界面", style);
            str1 = GUI.TextField(yhmposition, str1);
            str2 = GUI.PasswordField(mmposition, str2, '*');
            GUI.Label(yhmstr, "用户名:", yhmstyle);
            GUI.Label(mmstr, "密码:", mmstyle);
            if (GUI.Button(dlbutton, "登录"))
            {
                if (str1 == "admin" && str2 == "8888")
                {
                    SceneManager.LoadScene(1);
                }
            }
        }
    }
```
