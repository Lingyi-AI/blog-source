---
title: 7.滚动列表和分组
date: 2026-03-13 18:23
categories:
  - UI系统
  - IMGUI

tags: 
  - IMGUI（GUI）

---

### 知识点一 分组

``` CSharp
// 用于批量控制控件位置
// 可以理解为 包裹着的控件加了一个父对象
// 可以通过控制分组来控制包裹控件的位置	//开头结尾中间的为包裹要显示的内容
GUI.BeginGroup(groupPos);  //开头

GUI.Button(new Rect(0, 0, 100, 50), "测试按钮");
GUI.Label(new Rect(0, 60, 100, 20), "Label信息");

GUI.EndGroup();  //结尾
```


### 知识点二 滚动列表  

![图](images/Unity/UI系统/IMGUI（GUI）/7.滑动列表的两个范围.png)

``` CSharp
//这个也和上面一样要通过包裹使用
nowPos = GUI.BeginScrollView(scPos, nowPos, showPos);	//第一个参数和第三个参数是两个窗口范围

GUI.Toolbar(new Rect(0, 0, 300, 50), 0, strs);	//第二个是Vector2 是滚动条的位置值
GUI.Toolbar(new Rect(0, 60, 300, 50), 0, strs);
GUI.Toolbar(new Rect(120, 300, 50), 0, strs);
GUI.Toolbar(new Rect(180, 300, 50), 0, strs);

GUI.EndScrollView();
```

![图](images/Unity/UI系统/IMGUI（GUI）/7.习题.png)

``` CSharp
    private void OnGUI()
    {
        v2 = GUI.BeginScrollView(ckpos1, v2, ckpos2);

        for (int i = 0; i < strlabel.Length; i++)
        {
            GUI.Label(rects[i], strlabel[i]);
        }

        GUI.EndScrollView();
    }
```
