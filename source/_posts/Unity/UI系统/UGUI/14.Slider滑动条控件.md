---
title: 14.Slider滑动条控件
date:
categories:
  - UI系统
  - UGUI

tags: 
  - UGUI

---

### 知识点一 Slider是什么

Slider是滑动条组件
是UGUI中用于处理滑动条相关交互的关键组件

默认创建的Slider由4组对象组成
父对象——Slider组件依附的对象
子对象——背景图、进度图、滑动块三组对象

### 知识点二 Slider参数

![图](images/Unity/UI系统/UGUI/Slider参数.png)

### 知识点三 代码控制

``` CSharp
Slider s = this.GetComponent<Slider>();
print(s.value);
```

### 知识点四 监听事件的两种方式

#### 1.拖脚本

#### 括号里的参数 代表要添加函数时函数要有的参数 Single 就是 float

#### 代码添加时忘记了API可以查看Unity组件列表中的 On Value Changed

#### 滑动条滑动的时候触发事件

![图](images/Unity/UI系统/UGUI/Slider参数2.png)

#### 2.代码添加

#### UGUI中 列表事件添加移除函数用的都是这几个API
#### 添加函数 AddListener();
#### 移除函数 RemoveListener();
#### 移除所有函数 RemoveAllListeners();

#### 函数里的float 相当于组件参数 Value

``` CSharp
s.onValueChanged.AddListener((v) =>
{
    print("代码添加的监听" + v);
});
```

---

![图](images/Unity/UI系统/UGUI/Slider习题.png)

``` CSharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Buttonxiti : MonoBehaviour
{
    private GameObject zd;
    public Button but;
    public Toggle toggle;
    private GameObject obj;
    void Start()
    {
        zd = Resources.Load<GameObject>("Sphere1");
        but.onClick.AddListener(() => 
        {
            //创建子弹
            obj = Instantiate(zd, transform.position, transform.rotation);
            //根据滑动条控制音效大小
            obj.GetComponent<AudioSource>().volume = Silderxiti.silderxiti.slider.value;
            //是否开启音效
            if (toggle.isOn && obj != null)
            {
                AudioSource audio = obj.GetComponent<AudioSource>();
                Destroy(audio);
            }
        });

    }

}
```

``` CSharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Silderxiti : MonoBehaviour
{
    public static Silderxiti silderxiti;
    public Slider slider;
    void Start()
    {
        silderxiti = this;
    }
}
```