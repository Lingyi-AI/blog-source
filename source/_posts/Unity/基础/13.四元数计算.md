---
title: 13.四元数计算
date: 
categories:
  - 游戏引擎
  - Unity
  - 基础
tags: 
  - Unity

---

![图](images/Unity/基础/四元数计算主要学习内容.png)

### 知识点一 四元数相乘

``` CSharp
Quaternion q = Quaternion.AngleAxis(20, Vector3.up);
this.transform.rotation *= q;

this.transform.rotation *= q;
```
![图](images/Unity/基础/四元数相乘.png)


### 知识点二 四元数乘向量

``` CSharp
Vector3 v = Vector3.forward;
print(v);
v = Quaternion.AngleAxis(45, Vector3.up) * v;
print(v);
v = Quaternion.AngleAxis(45, Vector3.up) * v;
print(v);
// 四元数 * 向量 , 位置要是 四元数 * 向量 不然会报错
```

![图](images/Unity/基础/四元数乘向量.png)

![图](images/Unity/基础/四元数计算总结.png)

![图](images/Unity/基础/四元数计算习题.png)

### 习题一

#### 我:
``` CSharp
public GameObject zidan;
private E_fashezhuangtai e_f = E_fashezhuangtai.dan;

void Update()
{
    if (Input.GetMouseButtonDown(0)) 
    {
        fashefangfa();
    }
    if (Input.GetKeyDown(KeyCode.Alpha1))
    {
        e_f = E_fashezhuangtai.dan;
    }
    else if (Input.GetKeyDown(KeyCode.Alpha2)) 
    {
        e_f = E_fashezhuangtai.shuang;
    }
    else if (Input.GetKeyDown(KeyCode.Alpha3))
    {
        e_f = E_fashezhuangtai.shanxing;
    }
    else if (Input.GetKeyDown(KeyCode.Alpha4))
    {
        e_f = E_fashezhuangtai.huanxing;
    }
}
public void fashefangfa() 
{
    switch (e_f)
    {
        case E_fashezhuangtai.dan:
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right));
            break;
        case E_fashezhuangtai.shuang:
            Instantiate(zidan, transform.position + transform.right, Quaternion.AngleAxis(90, transform.right));
            Instantiate(zidan, transform.position - transform.right, Quaternion.AngleAxis(90, transform.right));
            break;
        case E_fashezhuangtai.shanxing:
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(-30, transform.forward));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(30, transform.forward));
            break;
        case E_fashezhuangtai.huanxing:
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(-180, transform.forward));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(-90, transform.forward));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(90, transform.forward));

            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(-45, transform.forward));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(-135, transform.forward));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(45, transform.forward));
            Instantiate(zidan, transform.position, Quaternion.AngleAxis(90, transform.right) * Quaternion.AngleAxis(135, transform.forward));
            break;
        default:
            break;
    }
}
enum E_fashezhuangtai 
{
    dan,
    shuang,
    shanxing,
    huanxing,
}
```

#### 唐:
``` CSharp
public enum E_FireType
{
    //单发
    One,
    //双发
    Two,
    //扇形
    Three,
    //环形
    Round
}

private E_FireType nowType = E_FireType.One;
public GameObject bullet;
public int roundNum = 4;

void Update()
{
    if( Input.GetKeyDown(KeyCode.Alpha1) )
    {
        nowType = E_FireType.One;
    }
    else if (Input.GetKeyDown(KeyCode.Alpha2))
    {
        nowType = E_FireType.Two;
    }
    else if (Input.GetKeyDown(KeyCode.Alpha3))
    {
        nowType = E_FireType.Three;
    }
    else if (Input.GetKeyDown(KeyCode.Alpha4))
    {
        nowType = E_FireType.Round;
    }

    if( Input.GetKeyDown(KeyCode.Space) )
    {
        Fire();
    }
}
private void Fire()
{
    switch (nowType)
    {
        case E_FireType.One:
            Instantiate(bullet, this.transform.position, this.transform.rotation);
            break;
        case E_FireType.Two:
            Instantiate(bullet, this.transform.position - this.transform.right * 0.5f, this.transform.rotation);
            Instantiate(bullet, this.transform.position + this.transform.right * 0.5f, this.transform.rotation);
            break;
        case E_FireType.Three:
            //朝自己的面朝向发射
            Instantiate(bullet, this.transform.position, this.transform.rotation);
            //朝自己左侧旋转20度再发射——知识点 四元数*四元数=一个新的四元数 相当于是旋转量的叠加
            Instantiate(bullet, this.transform.position, this.transform.rotation * Quaternion.AngleAxis(-20, Vector3.up));
            //朝自己左侧旋转20度再发射——知识点 四元数*四元数=一个新的四元数 相当于是旋转量的叠加
            Instantiate(bullet, this.transform.position, this.transform.rotation * Quaternion.AngleAxis(20, Vector3.up));
            break;
        case E_FireType.Round:
            float angle = 360 / roundNum;
            for (int i = 0; i < roundNum; i++)
                Instantiate(bullet, this.transform.position, this.transform.rotation * Quaternion.AngleAxis(i*angle, Vector3.up));
            break;
    }
}
```

### 习题二

#### 我:
``` CSharp
public GameObject MyCamera;
public float jiaodu = 45;
public float juli = 4;
public float shangfangjuli = 4;
public float min = 2;
public float max = 7;

void Update()
{
    juli += Input.GetAxis("Mouse ScrollWheel");
    juli = Mathf.Clamp(juli, min, max);
}

private void LateUpdate()
{
    //MyCamera.transform.position = Vector3.Lerp(MyCamera.transform.position, Quaternion.AngleAxis(jiaodu, transform.right) * transform.position + transform.up * shangfangjuli - transform.forward * juli, Time.deltaTime);
    //MyCamera.transform.rotation = Quaternion.Slerp(MyCamera.transform.rotation, Quaternion.LookRotation(-MyCamera.transform.position - transform.position), Time.deltaTime);
    //Debug.DrawLine(transform.position, Quaternion.AngleAxis(jiaodu, transform.right) * transform.position + transform.up * shangfangjuli - transform.forward * juli, Color.red);


    //MyCamera.transform.position = transform.position + transform.up * shangfangjuli + Quaternion.AngleAxis(jiaodu, transform.right) * -transform.forward * juli;
    MyCamera.transform.position = Vector3.Lerp(MyCamera.transform.position,
        transform.position + transform.up * shangfangjuli + Quaternion.AngleAxis(jiaodu, transform.right) * -transform.forward * juli ,
        Time.deltaTime);

    MyCamera.transform.rotation = Quaternion.Slerp(MyCamera.transform.rotation,
        Quaternion.LookRotation(-(MyCamera.transform.position - transform.position) / juli),
        Time.deltaTime);

    Debug.DrawLine(MyCamera.transform.position , transform.position , Color.red);

}
```

#### 唐:
``` CSharp
//目标对象
public Transform target;
//相对头顶的偏移位置
public float headOffsetH = 1;
//倾斜角度
public float angle = 45;
//默认的 摄像机离观测点的距离
public float dis = 5;

//距离必须是3和10之间
public float minDis = 3;
public float maxDis = 10;
//鼠标中间滚动控制的移动速度
public float roundSpeed = 1;
//看向对象时 四元数 旋转的速度
public float lookAtSpeed = 2;
//跟随对象移动的 速度
public float moveSpeed = 2;
//当前摄像机应该在的位置
Vector3 nowPos;

private Vector3 nowDir;

void Update()
{
    //实现了鼠标中键 滚动 来改变摄像机远近
    dis += Input.GetAxis("Mouse ScrollWheel")*roundSpeed;
    dis = Mathf.Clamp(dis, minDis, maxDis);

    //向头顶偏移位置
    nowPos = target.position + target.up * headOffsetH;
    //往后方偏移位置
    nowDir = Quaternion.AngleAxis(angle, target.right) * -target.forward;
    nowPos = nowPos + nowDir * dis;

    //直接把算出来的位置 进行赋值
    //this.transform.position = nowPos;
    this.transform.position = Vector3.Lerp(this.transform.position, nowPos, Time.deltaTime* moveSpeed);

    Debug.DrawLine(this.transform.position, target.position + target.up * headOffsetH);
    //这里是 通过插值运算 来缓动 看向 物体
    this.transform.rotation = Quaternion.Slerp(this.transform.rotation, Quaternion.LookRotation(-nowDir), Time.deltaTime* lookAtSpeed);
}
```