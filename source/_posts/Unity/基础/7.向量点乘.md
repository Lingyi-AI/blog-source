---
title: 7.向量点乘
date: 
categories:
  - 游戏引擎
  - Unity
  - 基础
tags: 
  - Unity

---

![图](images/Unity/基础/向量点乘主要学习内容.png)

### 补充知识 调试画线

``` CSharp
//画线段 
//前两个参数 分别是 起点 终点
Debug.DrawLine(this.transform.position, this.transform.position + this.transform.forward, Color.red);
//画射线
//前两个参数 分别是 起点 方向
Debug.DrawRay(this.transform.position, this.transform.forward, Color.white);
```

![图](images/Unity/基础/点乘计算公式.png)

### 知识点一 通过点乘判断对象方位

![图](images/Unity/基础/点乘几何意义.png)

``` CSharp
//Vector3 提供了计算点乘的方法
Debug.DrawRay(this.transform.position, this.transform.forward, Color.red);
Debug.DrawRay(this.transform.position, target.position - this.transform.position, Color.red);
//得到两个向量的点乘结果
//向量 a 点乘 AB 的结果
float dotResult = Vector3.Dot(this.transform.forward, target.position - this.transform.position);
if( dotResult >= 0 )
{
    print("它在我前方");
}
else
{
    print("它在我后方");
}
```


### 知识点二 通过点乘推导公式算出夹角

![图](images/Unity/基础/公式推导.png)

``` CSharp
//步骤
//1.用单位向量算出点乘结果
dotResult = Vector3.Dot(this.transform.forward, (target.position - this.transform.position).normalized);
//2.用反三角函数得出角度
print("角度-" + Mathf.Acos(dotResult) * Mathf.Rad2Deg);

//Vector3中提供了 得到两个向量之间夹角的方法 
print("角度2-" + Vector3.Angle(this.transform.forward, target.position - this.transform.position));
```

![图](images/Unity/基础/点乘总结.png)

![图](images/Unity/基础/点乘习题.png)

``` CSharp
public Transform t1;
private float f1;
private float f2;
private float f3;

void Update()
{
    //Debug.DrawLine(transform.position,t1.position - transform.position,Color.red);


    f1 = Vector3.Dot(transform.forward, (t1.position - transform.position).normalized); //点乘的两个参数要是单位向量（方向向量） 
    f2 = Mathf.Acos(f1) * Mathf.Rad2Deg;    //Acos得到的是弧度要转成角度
    f3 = (t1.position - transform.position).magnitude;  //通过模长判断距离距离
    if ( f2 <= 22.5 && f3 <= 5) 
    {
        Debug.Log("发现入侵者!");
    }

    //Debug.DrawRay(transform.position,transform.forward * 5,Color.red);
}
```