---
title: 7.特殊的引用类型
date: 2025-04-10 17:23
categories:
  - 编程语言
  - CSharp
  - 基础
tags: 
  - C#

---

### 知识点一 复习值和引用类型
值类型——它变我不变——存储在栈内存中
 无符号整形 有符号整形 浮点数 char bool 结构体（未学习）

引用类型——它变我也变——存储在堆内存中
 数组（一维、二维、交错） string 类（未学习）


### 知识点二 string的它变我不变

``` CSharp
string str1 = "123";
string str2 = str1;
//因为string是引用类型 按理说 应该是它变我也变
// string非常的特殊 它具备 值类型的特征 它变我不变
str2 = "321";

Console.WriteLine(str1);
Console.WriteLine(str2);

//string 虽然方便 但是有一个小缺点 就是频繁的 改变string 重新赋值
//会产生 内存垃圾
// 优化替代方案 我们会在 c#核心当中进行讲解
```

![图](images/CSharp/基础/特殊的引用类型string解释图.png)

---
#### 个人补充:
1. string 不可变（Immutable）
一旦创建，内容永远不能改
只要修改、拼接、替换，一定创建新对象
原来的字符串纹丝不动
2. 其他引用类型 可变（Mutable）
对象创建后，里面的值可以随便改
修改后还是同一个对象，不创建新的
多个引用指向同一个对象，改一个，全部都变！
