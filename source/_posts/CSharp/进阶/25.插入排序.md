---
title: 3.插入排序
date: 2025-12-13 10:55
categories:
  - 算法
  - 排序
tags: 
  - 排序算法
  - C#
---

### 知识点一 插入排序的基本原理
8 7 1 5 4 2 6 3 9
两个区域
排序区
未排序区
用一个索引值做分水岭

未排序区元素
与排序区元素比较
插入到合适位置
直到未排序区清空

![图](images/CSharp/进阶/插入排序理解图.png)

### 知识点二 代码实现  

``` CSharp
	//（插入排序基础版，易于个人理解）
            int[] arr = new int[] { 8,7,1,5,4,2,6,3,9};

            for (int i = 1; i < arr.Length; i++)
            {
                int pos = i - 1;
                while (pos >= 0 && arr[pos] > arr[pos+1])
                {
                    int v = arr[pos];
                    arr[pos] = arr[pos+1];
                    arr[pos+1] = v;
                    pos--;
                }
            }

            for (int i = 0; i < arr.Length; i++) 
            {
                Console.WriteLine(arr[i]);
            }


	//（插入排序优化版）
            int[] arr = new int[] { 8,7,1,5,4,2,6,3,9};

            for (int i = 1; i < arr.Length; i++)
            {
                int pos = i - 1;
                int v = arr[pos + 1];
                while (pos >= 0 && arr[pos] > v)
                {
                    arr[pos + 1] = arr[pos];
                    pos--;
                }
                arr[pos + 1] = v;
            }

            for (int i = 0; i < arr.Length; i++)
            {
                Console.WriteLine(arr[i]);
            }
```

---
### 知识点三总结

为什么有两层循环
第一层循环：一次取出未排序区的元素进行排序
第二层循环：找到想要插入的位置

为什么第一层循环从1开始遍历
插入排序的关键是分两个区域
已排序区 和 未排序区
默认第一个元素在已排序区

为什么使用while循环
满足条件才比较
否则证明插入位置已确定
不需要继续循环

为什么可以直接往后移位置
每轮未排序数已记录
最后一个位置不怕丢

为什么确定位置后，是放在sortIndex+1的位置
当循环停止时，插入位置应该是停止循环的索引加1处

基本原理
两个区域
用索引值来区分
未排序区与排序区
元素不停比较
找到合适位置
插入当前元素

套路写法
两层循环
一层获取未排序区元素
一层找到合适插入位置

注意事项
默认开头已排序
第二层循环外插入

