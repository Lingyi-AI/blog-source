---
title: 1.面向对象基本概念
date: 2025-08-01 14:23
categories:
  - 编程语言
  - CSharp
  - 核心
tags: 
  - C#

---

### 面向对象（OOP）三大特性

封装（Encapsulation）
将对象的状态（属性）和行为（方法）捆绑在一起，并对外部访问进行控制，隐藏内部实现细节，仅暴露必要的接口。
目的是提高代码安全性、可维护性和复用性。

继承（Inheritance）
允许一个类（子类 / 派生类）继承另一个类（父类 / 基类）的属性和方法，同时可以扩展或重写这些成员。
目的是实现代码复用，建立类之间的层次关系。

多态（Polymorphism）
指同一个操作作用于不同的对象会产生不同的结果。在 OOP 中，主要通过方法重写（Override）和方法重载（Overload）实现。
目的是提高代码的灵活性和可扩展性。


### 面向对象七大设计原则

开闭原则（Open-Closed Principle, OCP）
对扩展开放，对修改关闭。通过扩展来实现新功能，而非修改已有代码。

依赖倒转原则（Dependency Inversion Principle, DIP）
高层模块不依赖低层模块，二者都依赖抽象；抽象不依赖细节，细节依赖抽象。

里氏替换原则（Liskov Substitution Principle, LSP）
子类必须能够替换其基类，并且程序的行为不会发生改变。这是继承复用的基石。

单一职责原则（Single Responsibility Principle, SRP）
一个类应该只有一个引起它变化的原因，即只负责一项职责。

接口隔离原则（Interface Segregation Principle, ISP）
客户端不应该依赖它不需要的接口，应使用多个小而专一的接口，而非一个大而全的接口。

合成复用原则（Composite Reuse Principle, CRP）
优先使用对象组合 / 聚合，而非类继承来达到复用的目的。

迪米特法则（Law of Demeter, LoD）/ 最少知识原则
一个对象应当对其他对象有尽可能少的了解，只与直接的朋友通信，降低耦合度。

![图](images/CSharp/核心/面向对象基本概念图.png)
![图](images/CSharp/核心/面向对象三大特征七大原则.png)